n = len(nums)
suffix = 1
prefix = 1
res = [1]*n

for i in range(n):
  res[i] = prefix
  prefix = prefix * nums[i]

for i in range(n-1, -1, -1):
  res[i] = res[i] * suffix
  suffix = suffix*nums[i]

print(res)
  
