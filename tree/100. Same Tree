class Solution:
    def isSameTree(self, p: Optional[TreeNode], q: Optional[TreeNode]) -> bool:leTree(p.right, q.right)
        if not p and not q:
            return True
        ir not p or not q:
            return False
        return p.val==q.val and elf.isSameTree(p.left, q.left) and isSameTree(p.right, q.right)
